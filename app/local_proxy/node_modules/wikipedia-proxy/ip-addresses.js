const os = require("os")

const ifaces = os.networkInterfaces()

const a = Object.keys(ifaces)

/** @type {string[]} */
module.exports = (
    a.map(name => ifaces[name]
        .filter(x => x.family == "IPv4")
        .map(x => x.address)
        .reduce((l, x) => l.concat(x))
    )
)